# >=1.12 for the resource-server auth API (TokenVerifier + AuthSettings.resource_server_url,
# used by the streamable-http OAuth mode). httpx for the REST + token-verification calls.
#
# Capped below 2.0: the 2.x SDK dropped `mcp.server.fastmcp`, which this server is built on, so an
# unpinned rebuild crash-loops the container on ModuleNotFoundError. Porting to the 2.x server API
# is a deliberate migration, not something to pick up implicitly from a rebuild.
mcp>=1.12,<2
httpx>=0.27

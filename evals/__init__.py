"""SkillHub skill-evaluation sandbox (Approach A: local, stdlib-only).

A dependency-free harness that stands up fixtures + a recording fake service so the developer's
live Claude Code can execute a skill against them and the harness can score its effectiveness.
See README.md; the hosted variant is sketched in docs/skill-sandbox-approach-C.md."""

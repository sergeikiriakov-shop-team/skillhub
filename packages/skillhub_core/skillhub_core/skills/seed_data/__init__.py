"""Seed fixtures shipped with the package (e.g. the demo green-loop sandbox trial notebook).

Kept as a package so setuptools installs the non-Python data files alongside the code (declared in
``pyproject.toml`` under ``[tool.setuptools.package-data]``)."""
